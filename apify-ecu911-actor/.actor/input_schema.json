{
  "title": "ECU 911 cache input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "storeName": {
      "title": "Key-value store name",
      "type": "string",
      "description": "Named Apify key-value store where the latest cache will be saved.",
      "default": "ecu911-morona-santiago-cache"
    },
    "recordKey": {
      "title": "Record key",
      "type": "string",
      "description": "Record key used by the Vercel API to read the latest cache.",
      "default": "latest"
    },
    "useApifyProxy": {
      "title": "Use Apify Proxy",
      "type": "boolean",
      "description": "Enable if ECU 911 blocks Apify datacenter requests.",
      "default": false
    },
    "proxyGroups": {
      "title": "Proxy groups",
      "type": "array",
      "description": "Optional Apify Proxy groups, for example RESIDENTIAL if available in your plan.",
      "editor": "stringList",
      "default": []
    }
  }
}
